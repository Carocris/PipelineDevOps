# .github/workflows/ci.yml
name: 🚀 CI - Pipeline DevOps

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: 🛠️ Backend CI
    runs-on: ubuntu-latest

    services:
      mssql:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          SA_PASSWORD: "ccoa1234!"
          ACCEPT_EULA: "Y"
        ports:
          - 1433:1433
        options: >-
          --health-cmd "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P 'ccoa1234!' -Q 'SELECT 1'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 20s

    steps:
      - name: 🔄 Checkout repositorio
        uses: actions/checkout@v3

      - name: 📁 Navegar al backend e instalar dependencias
        working-directory: backend
        run: npm install

      - name: 🧪 Ejecutar pruebas
        working-directory: backend
        env:
          DB_USER: sa
          DB_PASSWORD: ccoa1234!
          DB_SERVER: localhost
          DB_NAME: DevOpsDB
        run: npm test

      - name: 📦 Confirmar ejecución del pipeline
        run: echo "✅ CI ejecutado correctamente desde GitHub Actions"
